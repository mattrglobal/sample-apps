version: '3.8'

services:
  app:
    container_name: webhook-app
    platform: linux/arm64
    build: 
      context: .
      args:
        PORT: ${PORT:-7311}
    ports:
      - "${PORT:-7311}:${PORT:-7311}"
    env_file: .env
    environment:
      - PORT=${PORT:-7311}
    volumes:
      - .:/app
      - /app/node_modules
    restart: unless-stopped
