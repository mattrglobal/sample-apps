<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- Registers a custom URL scheme so the wallet app can redirect back to this
	     verifier with the presentation result. The scheme is bound to the app's
	     bundle identifier via $(PRODUCT_BUNDLE_IDENTIFIER), so it automatically
	     matches whatever bundle identifier you set on the target. The redirect URI
	     you configure for the verifier application in your MATTR VII tenant must use
	     this same scheme, e.g. {bundleId}://my/path -->
	<key>CFBundleURLTypes</key>
	<array>
		<dict>
			<key>CFBundleTypeRole</key>
			<string>Editor</string>
			<key>CFBundleURLName</key>
			<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
			<key>CFBundleURLSchemes</key>
			<array>
				<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
			</array>
		</dict>
	</array>
</dict>
</plist>
